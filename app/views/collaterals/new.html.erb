<h1>Add new Collateral</h1>
<%= CollateralType.collateral_type[:article]%>
<div class = 'col-md-8 offset-2'>
  <h1 class = "text-center">New Blog Post</h1>
  <%= simple_form_for @collateral do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :url %>
    // <%= f.input :collateral_type %>
    <%#= f.input :tag_ids, as: :select, collection: Tag.order(:name), label_method: :name, input_html: {multiple: true} %>
    <%= f.submit "Add", class: 'btn btn-lg btn-primary float-right' %>
    <%#= select("tag", "name", @tags) %>
  <% end %>
</div>
